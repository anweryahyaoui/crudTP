<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>classroom list</title>
</head>
<body>
{% for i in list %}

    {{ i.id }}

    {{ i.name }}
    <a href="{{ path('app_classroomlist_classroom', {id: i.id}) }}">delete</a>
    <a href="{{ path('app_classroomlist_classroom', {id: i.id}) }}">update</a>
    <br>

{% endfor %}



</body>
</html>